import FavoriteShowsList from '../FavoriteShowsList';

const Sidebar = () => {
  return (
    <section className="mt-16 md:mt-0 md:border-l-2 h-full">
      <FavoriteShowsList />
    </section>
  );
};

export default Sidebar;